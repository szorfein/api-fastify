{
    "type": "object",
    "$id": "schema:auth:credential:response",
    "required": ["id"],
    "additionalProperties": false,
    "properties": {
        "id": {
            "type": "string",
            "format": "uuid"
        }
    }
}
